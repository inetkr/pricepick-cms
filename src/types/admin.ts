export type IAdmin = {
    id: string;
    username: string;
    name: string;
    role: string;
    email: string;
    phoneNumber: string;
    isActive: boolean;
    createdAt: string;
    updatedAt: string;
}